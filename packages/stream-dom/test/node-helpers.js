suite(`nodes/util`, function () {
  suite(`createNodeDescriptors`, function () {
    test(`creating nodes from declarations`)
    test(`creating replacement stream nodes from streams`)
    test(`creating text nodes by default`)
    test(`expanding array of declarations in-place`)
  })
})
